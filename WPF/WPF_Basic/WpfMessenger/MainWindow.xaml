<Window x:Class="WpfMessenger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMessenger"
        xmlns:views="clr-namespace:WpfMessenger.Views"
        xmlns:vms="clr-namespace:WpfMessenger.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type vms:SenderViewModel}">
            <views:SenderView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vms:Receiver1ViewModel}">
            <views:Receiver1View/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vms:Receiver2ViewModel}">
            <views:Receiver2View/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <ContentControl Grid.Column="0"
                        Margin="5"
                        Content="{Binding SenderVM}"/>

        <ContentControl Grid.Column="1"
                        Margin="5"
                        Content="{Binding Receiver1VM}"/>

        <ContentControl Grid.Column="2"
                        Margin="5"
                        Content="{Binding Receiver2VM}"/>
    </Grid>

</Window>
