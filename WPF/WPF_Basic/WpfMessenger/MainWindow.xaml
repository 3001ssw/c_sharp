<Window x:Class="WpfMessenger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMessenger"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:SenderViewModel}">
            <local:SenderView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:ReceiveViewModel}">
            <local:ReceiverView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:OtherReceiverViewModel}">
            <local:OtherReceiverView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <ContentControl Grid.Column="0"
                        Content="{Binding SenderVM}"/>

        <ContentControl Grid.Column="1"
                        Content="{Binding ReceiverVM}"/>

        <ContentControl Grid.Column="2"
                        Content="{Binding OtherReceiverVM}"/>
    </Grid>

</Window>
