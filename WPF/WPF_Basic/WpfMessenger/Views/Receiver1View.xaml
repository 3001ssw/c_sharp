<UserControl x:Class="WpfMessenger.Views.Receiver1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfMessenger.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!--#region Sender-->
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="Sender"/>
        <TextBlock Grid.Row="0"
                   Grid.Column="1"
                   Text="{Binding SendObject, UpdateSourceTrigger=PropertyChanged}"/>
        <!--#endregion-->

        <!--#region Receive Text-->
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Text="Message"/>
        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Text="{Binding ReceiveText, UpdateSourceTrigger=PropertyChanged}"/>
        <!--#endregion-->
    </Grid>
</UserControl>
