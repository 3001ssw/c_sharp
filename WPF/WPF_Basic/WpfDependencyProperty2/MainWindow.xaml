<Window x:Class="WpfDependencyProperty2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDependencyProperty2"
        xmlns:b="clr-namespace:WpfDependencyProperty2.Behavior"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:cvt="clr-namespace:WpfDependencyProperty2.Converter"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.Resources>
        <cvt:StringToBase64Converter x:Key="StringToBase64Converter"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0"
                  ItemsSource="{Binding Files}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  VirtualizingStackPanel.ScrollUnit="Pixel"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  SelectionUnit="FullRow">
            <i:Interaction.Behaviors>
                <b:DragDropBehavior FilesDropCommand="{Binding FilesDropCommand}"/>
            </i:Interaction.Behaviors>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding FileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="100"/>
                <DataGridTextColumn Header="Path"
                                    Binding="{Binding FilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Column="1"
                      Margin="2"
                      Width="5"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Center"
                      Background="DarkGray"/>

        <Border Grid.Column="2"
                Background="Transparent"
                AllowDrop="True">
            <i:Interaction.Behaviors>
                <b:DragDropBehavior SerializableDragCommand="{Binding ImageFileDropCommand}"/>
            </i:Interaction.Behaviors>

            <Image Source="{Binding Base64String, Converter={StaticResource StringToBase64Converter}}"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Stretch="Fill"/>
        </Border>
    </Grid>
</Window>
