<Window x:Class="WpfDialogService.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDialogService"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <StackPanel Margin="10">
        <Button Content="ShowDialog T"
                Margin="5"
                Command="{Binding ShowDialogCommand}"/>
        <Button Content="Show T"
                Margin="5"
                Command="{Binding ShowCommand}"/>
        <Button Content="ShowDialogActivator"
                Margin="5"
                Command="{Binding ShowDialogActivatorCommand}"/>
        <Button Content="ShowDialogDataTemplate"
                Margin="5"
                Command="{Binding ShowDialogDataTemplateCommand}"/>
    </StackPanel>
</Window>
