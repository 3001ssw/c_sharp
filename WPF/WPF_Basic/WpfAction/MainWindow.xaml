<Window x:Class="WpfAction.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAction"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.Resources>
        <local:DirectEnumToBoolConverter x:Key="DirectEnumToBoolConverter"/>
    </Window.Resources>
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Grid.Column="0"
              Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <RadioButton Grid.Row="0"
                         Grid.Column="1"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Content="위"
                         GroupName="DirectGroup"
                         IsChecked="{Binding SelectedDirect, Converter={StaticResource DirectEnumToBoolConverter}, ConverterParameter=Top}"/>
            <RadioButton Grid.Row="1"
                         Grid.Column="0"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Content="왼쪽"
                         GroupName="DirectGroup"
                         IsChecked="{Binding SelectedDirect, Converter={StaticResource DirectEnumToBoolConverter}, ConverterParameter=Left}"/>
            <RadioButton Grid.Row="1"
                         Grid.Column="2"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Content="오른쪽"
                         GroupName="DirectGroup"
                         IsChecked="{Binding SelectedDirect, Converter={StaticResource DirectEnumToBoolConverter}, ConverterParameter=Right}"/>
            <RadioButton Grid.Row="2"
                         Grid.Column="1"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Content="아래"
                         GroupName="DirectGroup"
                         IsChecked="{Binding SelectedDirect, Converter={StaticResource DirectEnumToBoolConverter}, ConverterParameter=Bottom}"/>
            <Button Grid.Row="1"
                    Grid.Column="1"
                    Margin="5"
                    Content="Move"
                    Command="{Binding MoveCommand}"/>
        </Grid>

        <DataGrid Grid.Row="1"
                  Margin="5"
                  ItemsSource="{Binding Messages}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="All"
                  CanUserReorderColumns="False"
                  CanUserSortColumns="False"
                  IsReadOnly="True"
                  VirtualizingStackPanel.ScrollUnit="Pixel"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time"
                                    Binding="{Binding Time, StringFormat='{}HH:mm:ss.fff'}"
                                    Width="150"/>
                <DataGridTextColumn Header="Message"
                                    Binding="{Binding Message}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
