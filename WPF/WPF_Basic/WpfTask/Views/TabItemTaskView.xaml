<UserControl x:Class="WpfTask.Views.TabItemTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfTask.Views"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:b="clr-namespace:WpfTask.Behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="1초마다 1~10 출력"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="5"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="5">
            <Button Content="Start"
                    Command="{Binding StartCommand}"
                    Width="70"
                    Margin="0,0,8,0"/>
            <Button Content="Pause"
                    Command="{Binding PauseCommand}"
                    Width="70"
                    Margin="0,0,8,0"/>
            <Button Content="Resume"
                    Command="{Binding ResumeCommand}"
                    Width="70"
                    Margin="0,0,8,0"/>
            <Button Content="Stop"
                    Command="{Binding StopCommand}"
                    Width="70"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  Margin="5"
                  ItemsSource="{Binding Logs}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="All"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  VirtualizingStackPanel.ScrollUnit="Pixel"
                  VirtualizingStackPanel.VirtualizationMode="Recycling">
            <i:Interaction.Behaviors>
                <b:DataGridBehavior IsAutoScroll="True"/>
            </i:Interaction.Behaviors>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time"
                                    Binding="{Binding Time, StringFormat='{}HH:mm:ss.fff'}"
                                    Width="150"/>
                <DataGridTextColumn Header="Message"
                                    Binding="{Binding Message}"
                                    Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
