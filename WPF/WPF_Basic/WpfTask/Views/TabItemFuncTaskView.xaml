<UserControl x:Class="WpfTask.Views.TabItemFuncTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfTask.Views"
             xmlns:cvt="clr-namespace:WpfTask.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <cvt:FuncTypeToBoolConverter x:Key="FuncTypeToBoolConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0"
                    Grid.Column="0"
                    Orientation="Horizontal">
            <RadioButton GroupName="DoFuntion"
                         IsChecked="{Binding SelectedFunctionType, Converter={StaticResource FuncTypeToBoolConverter}, ConverterParameter=Func1}"
                         Content="Function 1"
                         Margin="5"/>
            <RadioButton GroupName="DoFuntion"
                         IsChecked="{Binding SelectedFunctionType, Converter={StaticResource FuncTypeToBoolConverter}, ConverterParameter=Func2}"
                         Content="Function 2"
                         Margin="5"/>

            <Button Content="Test 1"
                    Width="100"
                    Margin="5"
                    Command="{Binding Test01Command}"/>
        </StackPanel>
        <ListBox Grid.Row="1"
                 Grid.Column="0"
                 ItemsSource="{Binding Logs}"/>
    </Grid>
</UserControl>
