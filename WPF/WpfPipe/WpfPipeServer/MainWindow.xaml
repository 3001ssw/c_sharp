<Window x:Class="WpfPipeServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfPipeServer"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Margin="5"
                   Text="Server"/>

        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Margin="5"
                   Text="pipe name"/>
        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Margin="5"
                 Text="{Binding PipeName}"/>
        <Button Grid.Row="1"
                Grid.Column="2"
                Margin="5"
                Content="Create"
                Command="{Binding NamedPipeServerCreateCommand}"/>
        <Button Grid.Row="1"
                Grid.Column="3"
                Margin="5"
                Content="Close"
                Command="{Binding NamedPipeServerCloseCommand}"/>

        <DataGrid Grid.Row="2"
                  Grid.Column="0"
                  Grid.ColumnSpan="4"
                  Margin="5"
                  ItemsSource="{Binding Messages}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="All"
                  VirtualizingStackPanel.ScrollUnit="Pixel"
                  VirtualizingStackPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time"
                                    Binding="{Binding Time, StringFormat='{}HH:mm:ss.fff'}"
                                    Width="150"/>
                <DataGridTextColumn Header="Message"
                                    Binding="{Binding Message}"
                                    Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
