<Window x:Class="TaskTest01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskTest01"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Width="100"
                    Margin="5"
                    Command="{Binding StartAddCommand}"
                    Content="Add Start"/>
            <Button Width="100"
                    Margin="5"
                    Command="{Binding StopAddCommand}"
                    Content="Add Stop"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Width="100"
                    Margin="5"
                    Command="{Binding ParallelStartCommand}"
                    Content="Parallel Start"/>
            <Button Width="100"
                    Margin="5"
                    Command="{Binding ParallelStopCommand}"
                    Content="Parallel Stop"/>
        </StackPanel>
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Students}"
                  Margin="0">
        </DataGrid>
    </Grid>
</Window>
